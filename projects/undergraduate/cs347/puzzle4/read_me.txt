To compile this just give the command 
> make
or 
> make driver

To run with the supplied files, then, just
> ./driver <filename>

Suggested to run with problem<4, 5, or 6>.tsp instead of problem<1, 2, 3>.tsp